# Runlayer Hero

The supplied Runlayer Hero project contained only `dist`, `node_modules`, and planning files—no editable application source. Its verified compiled build is preserved at `public/tools/runlayer-hero` and is loaded by the shared host from there.
